# RFC-0031: MCP

Status: Draft — revision specified by D30, not yet applied

> **The security section below is superseded in three places.** `docs/decisions.md` D30 settles
> them: a server's authority is fixed when it is enabled and it may never raise a capability
> request; the grant is by effect class at enable time; the `TRUSTED` promotion in §7 is removed;
> nothing spawns on project open. The Abstract's "stdio and streamable HTTP" also contradicts the
> MVP scope below and D17. Do not implement against this document until the revision lands.

## Abstract

MCP (Model Context Protocol) is an open standard protocol for AI assistants to interact with tools and data sources. Aidos supports MCP in Version 1 by bridging MCP servers into the Tool Broker (RFC-0030). MCP servers advertise capabilities (tools, resources, sampling); Aidos translates them to Tool Broker tools. MCP support uses stdio and streamable HTTP transports. MCP is an adapter, not a replacement for the Tool Broker—all tools, MCP-sourced or native, flow through the unified Tool Broker.

## Motivation

MCP is an emerging standard that allows AI systems to discover and use tools in a standardized way. Supporting MCP enables:

1. **Ecosystem integration**: Use existing MCP servers (GitHub, Google, Slack).
2. **Extensibility**: Anyone can build an MCP server; Aidos can use it.
3. **Interoperability**: Aidos tools can be MCP servers used by other systems.
4. **Future-proof**: MCP may become the standard for AI tool discovery.

Aidos supports MCP as an adapter: MCP servers are translated into Tool Broker tools.

## Goals

1. **Define MCP integration**: How do MCP servers become Aidos tools?

2. **Specify transport support**: stdio and HTTP streaming.

3. **Establish capability mapping**: How do MCP capabilities become Tool Broker capabilities?

4. **Define tool discovery**: How does Aidos find and load MCP servers?

5. **Clarify lifecycle**: When are MCP servers started/stopped?

6. **Explain future server support**: Building MCP servers for Aidos.

## Non-goals

This RFC does not implement MCP. It defines architectural support.

This RFC does not mandate all tools be MCP servers (native tools are fine).

This RFC does not address MCP client library details (implementation).

## Design

### Registration is user-scope; projects only request

**A project may never define the command that starts an MCP server.** MCP servers are
registered at user scope (RFC-0054) and enabled per project. A project config may *request* a
server by name; the user installs and configures it once.

This closes a critical hole. Previously servers were discovered from project configuration
including a `command` string, and project configuration is Git-tracked — so cloning or
importing a project was arbitrary code execution on first use. "Discover MCP servers on PATH"
is removed for the same reason: it makes the runtime's behaviour depend on ambient environment
state that a project could influence.

User-scope registration (`~/.aidos/mcp/servers.toml`), written only through an explicit user
action:

```toml
[[server]]
name       = "github"
command    = "npx"
args       = ["-y", "@modelcontextprotocol/server-github"]
transport  = "stdio"
profiles   = ["DESKTOP", "HEADLESS_SERVER"]
secret_ref = "github_token"        # a reference into the vault (RFC-0035), never a value
```

Project request (`aidos.toml`, Git-tracked):

```toml
[mcp]
requested = ["github"]             # names only. no commands, no arguments, no secrets.
```

Opening a project that requests an unregistered server reports it as an unsatisfied requirement
(RFC-0049) and offers to install it. It never installs one silently.

**Secrets are never inline.** `secret_ref` names a vault entry; the value is injected into the
child process environment at spawn time and never written to any config file. An earlier
example in this RFC contained a literal `"STRIPE_API_KEY": "sk_..."` inside project
configuration that RFC-0017 declares Git-authoritative — a documented path to committing a live
credential.

### Transport Support

Aidos supports two MCP transport mechanisms:

#### Stdio Transport

MCP server communicates via stdin/stdout. Aidos:

1. Starts MCP server as subprocess.
2. Communicates via JSON-RPC over stdin/stdout.
3. Captures server capabilities.
4. Routes requests to server.
5. Manages server lifecycle (restart, shutdown).

#### HTTP Streaming Transport

MCP server exposes HTTP endpoint. Aidos:

1. Connects to HTTP endpoint.
2. Uses Server-Sent Events (SSE) for streaming.
3. Sends requests via HTTP POST.
4. Handles backpressure and reconnection.

### Capability Mapping

MCP servers advertise capabilities (tools, resources, sampling). Aidos maps these to Tool Broker interface:

```
MCP Tool: "get_github_issues"
  Input: { repo: String, state: String }
  Output: { issues: List<Issue> }
  
Aidos Tool Broker:
  Tool: "github_mcp"
  Capability: "github:list_issues"
  invoke(capability_id, request) →
    Call MCP server "get_github_issues"
    Return result
```

### Request/Response Flow

```
Session requests: "List GitHub issues for org/repo"

Tool Broker:
  1. Routes to tool "github_mcp"
  2. Maps to MCP tool "get_github_issues"
  3. Sends MCP request over transport
  4. MCP server executes
  5. Returns result
  6. Tool Broker returns to session
  7. Publishes ToolCompleted event
```

### MCP Resources and Sampling

MCP supports resources (files, config) and sampling (prompt templates). Aidos:

- **Resources**: Exposed via filesystem tool (sessions can read MCP resources as files).
- **Sampling**: Not directly used (MCP sampling is for LLM input preparation; Aidos handles this differently).

### Error Handling

MCP server failures are handled by Tool Broker:

```
MCP server crashes:
  Tool Broker detects disconnection
  Logs error
  Publishes ToolFailed event
  Optionally restarts server (configurable)
  Session receives error
```

## Data Model (Conceptual)

```
MCPServerRegistry {
  servers: Map<ServerName, MCPServerConfig>
  
  live_servers: Map<ServerName, MCPServerConnection>
  
  tool_mappings: Map<MCPToolId, ToolBrokerCapability>
}

MCPServerConfig {                   # user scope only (RFC-0054)
  name: String
  command: String                   # how to start the server
  args: List<String>
  transport: String                 # "stdio" | "http"
  profiles: Set<PlatformProfile>    # where it is available (RFC-0049)

  endpoint_url: String?             # HTTP transport
  secret_refs: Map<String, SecretId>  # env var name -> vault entry (RFC-0035).
                                      # never literal values.
  trust: TrustLevel                 # UNVERIFIED by default
  startup_timeout: Duration
  auto_restart: Boolean
}

MCPServerConnection {
  server_id: String
  transport: TransportConnection
  status: ConnectionStatus          # "connected", "disconnected"
  
  capabilities: List<MCP Capability>
  
  connected_at: Timestamp
  last_heartbeat: Timestamp
}
```

## Security

MCP is the first extension mechanism Aidos ships, so it carries the trust model that plugins
will later reuse.

1. **MCP servers are capability subjects.** An MCP server holds its own attenuated grant
   (`SubjectKind.MCP_SERVER`, RFC-0018) rather than borrowing the calling session's authority.
   Without this, a hostile server inherits whatever the session holds — the confused-deputy
   problem one level out.

2. **A separate process is not isolation.** A stdio child runs with the same user ID and the
   same filesystem and network reach as the runtime. Stating subprocess execution as an
   isolation mechanism was misleading. Actual containment on DESKTOP uses the platform sandbox
   where available; on all profiles, the meaningful controls are the attenuated grant, the
   scrubbed environment, and audit.

3. **Scrubbed environment.** Spawned servers receive no runtime connection token and no socket
   path (RFC-0055). Without this, a server could connect to the runtime that spawned it and
   approve its own pending capability request.

4. **Tool results are UNTRUSTED** (RFC-0027) and taint the Run, attenuating its authority for
   subsequent steps. This is the primary defence against a server returning content crafted to
   steer the model.

5. **Tool schemas are validated on receipt.** A server's advertised JSON Schema is checked
   before its operations are offered to a model, and operation names are namespaced by server
   so two servers cannot collide or shadow a built-in tool.

6. **Credentials by reference.** Secrets come from the vault (RFC-0035) via `secret_ref` and
   are injected at spawn. They never enter project configuration or the audit log.

7. **Trust levels.** A registered server is `UNVERIFIED` by default. The user may mark a server
   `TRUSTED`, which only relaxes per-call approval for `Egress`; it never removes the capability
   check or the taint.

### Platform availability (RFC-0049)

| Transport | MOBILE | DESKTOP | HEADLESS_SERVER |
|---|---|---|---|
| stdio | **unavailable** | yes | yes |
| Streamable HTTP | yes, online only | yes | yes |

Android cannot spawn arbitrary interpreters or binaries, so stdio MCP does not exist on
MOBILE — and HTTP MCP requires connectivity, so it is unavailable offline. **The core mobile
use case must not depend on MCP at all.** Filesystem, Git, and bundled tools carry it. MCP is
an enrichment on capable, connected devices, not a foundation.

### MCP lands in the MVP, on desktop

MCP stdio ships with the **first vertical slice** (RFC-0099 Phase 2), alongside the CLI — not in
a later ecosystem phase.

Two reasons, and the second is the stronger one:

1. The MVP is CLI-first and therefore DESKTOP, which is exactly where stdio MCP works. There is
   no platform obstacle to having it early.
2. **It validates the tool abstraction while that is still cheap to change.** MCP is the first
   real test of whether `ToolDescriptor`, the effect taxonomy, and the capability-subject model
   can absorb tools the runtime did not write. If they cannot, that is a finding worth having in
   month four rather than month fourteen, when every built-in tool has been written against the
   assumption.

The cost is smaller than it appears: `MCP_SERVER` capability subjects (RFC-0018), the scrubbed
child environment (RFC-0055), taint on tool results (RFC-0027), and schema validation on receipt
are all already MVP items for other reasons. What is genuinely added is the stdio transport and
server lifecycle.

**This does not change the plugin decision.** MCP is a protocol spoken to a separate process
that the user installed deliberately; a plugin host is arbitrary code loaded into the runtime.
They are different trust problems, and RFC-0043 still defers the plugin host past v1.

**Consequence to accept:** the MVP is no longer purely first-party. From Phase 2 onward the
runtime executes code it did not write, so the MCP trust model — user-scope registration,
attenuated grants, scrubbed environment, untrusted results — is MVP-critical rather than
future hardening.

## MVP Scope

MVP includes (Phase 2, DESKTOP only):

1. **Stdio transport**: spawn and communicate with MCP servers, with lifecycle management.
2. **User-scope registration**; projects request servers by name only.
3. **Operation discovery** with JSON Schema validation on receipt; names namespaced per server.
4. **`MCP_SERVER` capability subjects** holding attenuated grants (RFC-0018).
5. **Scrubbed child environment** — no runtime token, no socket path (RFC-0055).
6. **Results are `UNTRUSTED`** and taint the Run (RFC-0027).
7. **Secrets by `secret_ref`**, injected at spawn (RFC-0035).
8. Crash and timeout handling; audit through the Tool Broker.

Not included:

- MCP on MOBILE — unavailable by platform (RFC-0049).
- HTTP transport (v1.x, when connectivity-dependent tools matter).
- MCP resources and sampling.
- Aidos exposing itself as an MCP server.

## Future Work

### HTTP Transport

Support MCP servers over HTTP with SSE.

### MCP Server Building

Aidos publishes SDK for building MCP servers:

```python
from aidos.mcp import Server

server = Server("my-tool")

@server.tool()
def process_data(input_data: str) -> str:
    return f"Processed: {input_data}"

server.run()
```

### MCP Resource Support

Integrate MCP resources into Knowledge Engine:

```
MCP server provides: "company_handbook" resource
Knowledge Engine indexes: handbook content
Sessions can search: handbook via semantic search
```

### MCP Marketplace

Curated list of MCP servers compatible with Aidos:

```
"GitHub MCP" (4.8★)
"Stripe MCP" (4.6★)
"Linear MCP" (4.7★)
```

### MCP Debugging

Tools to debug MCP server issues:

```
aidos mcp test github-server
  → Start server
  → List capabilities
  → Attempt sample tool call
  → Report results
```

## Resolved questions

These were open questions. Since MCP is in V1 and is the first code Aidos runs that it did not
write, leaving its security model open was not tenable.

- **May MCP servers have side effects?** Yes, through declared typed effects (RFC-0030). An
  operation that mutates or egresses is treated exactly like a built-in one.
- **Trust model?** Yes: `UNVERIFIED` by default, user-promotable to `TRUSTED`, which relaxes
  per-call `Egress` approval only.
- **Sandboxing?** Attenuated capability grant on all profiles; platform sandbox on DESKTOP
  where available; scrubbed environment always. Subprocess alone is not treated as isolation.
- **Version conflicts between servers?** Operations are namespaced by server name
  (`github.list_issues`), so two servers cannot collide. Built-in tools always win a bare name.
- **Authentication / OAuth?** Vault-held credentials by reference. Interactive OAuth flows are
  out of scope for V1: a server needing one is configured out-of-band by the user.

## Open Questions

- Should Aidos expose itself as an MCP server, allowing other AI systems to drive it? This is
  an inbound authority surface and would need its own capability subject and threat analysis.
- Should MCP resources feed the Knowledge Engine, and if so with what trust level and
  invalidation policy?
