# RFC-0031: MCP

Status: Accepted

## Abstract

MCP (Model Context Protocol) is an open standard protocol for AI assistants to interact with tools and data sources. Aidos supports MCP in Version 1 by bridging MCP servers into the Tool Broker (RFC-0030). MCP servers advertise capabilities (tools, resources, sampling); Aidos translates them to Tool Broker tools. MCP support uses stdio and streamable HTTP transports. MCP is an adapter, not a replacement for the Tool Broker—all tools, MCP-sourced or native, flow through the unified Tool Broker.

## Motivation

MCP is an emerging standard that allows AI systems to discover and use tools in a standardized way. Supporting MCP enables:

1. **Ecosystem integration**: Use existing MCP servers (GitHub, Google, Slack).
2. **Extensibility**: Anyone can build an MCP server; Aidos can use it.
3. **Interoperability**: Aidos tools can be MCP servers used by other systems.
4. **Future-proof**: MCP may become the standard for AI tool discovery.

Aidos supports MCP as an adapter: MCP servers are translated into Tool Broker tools.

## Goals

1. **Define MCP integration**: How do MCP servers become Aidos tools?

2. **Specify transport support**: stdio and HTTP streaming.

3. **Establish capability mapping**: How do MCP capabilities become Tool Broker capabilities?

4. **Define tool discovery**: How does Aidos find and load MCP servers?

5. **Clarify lifecycle**: When are MCP servers started/stopped?

6. **Explain future server support**: Building MCP servers for Aidos.

## Non-goals

This RFC does not implement MCP. It defines architectural support.

This RFC does not mandate all tools be MCP servers (native tools are fine).

This RFC does not address MCP client library details (implementation).

## Design

### MCP Server Discovery and Loading

Aidos discovers MCP servers from:

1. **Project configuration**: Defined in project config.
2. **Plugin system**: MCP servers installed via plugins (RFC-0060).
3. **Environment**: MCP servers on PATH.

Example configuration:

```json
{
  "mcp_servers": [
    {
      "name": "github-mcp",
      "command": "python -m mcp.server.github",
      "transport": "stdio",
      "auth": { "token_env": "GITHUB_TOKEN" }
    },
    {
      "name": "stripe-mcp",
      "command": "stripe-mcp",
      "transport": "stdio",
      "env": { "STRIPE_API_KEY": "sk_..." }
    }
  ]
}
```

### Transport Support

Aidos supports two MCP transport mechanisms:

#### Stdio Transport

MCP server communicates via stdin/stdout. Aidos:

1. Starts MCP server as subprocess.
2. Communicates via JSON-RPC over stdin/stdout.
3. Captures server capabilities.
4. Routes requests to server.
5. Manages server lifecycle (restart, shutdown).

#### HTTP Streaming Transport

MCP server exposes HTTP endpoint. Aidos:

1. Connects to HTTP endpoint.
2. Uses Server-Sent Events (SSE) for streaming.
3. Sends requests via HTTP POST.
4. Handles backpressure and reconnection.

### Capability Mapping

MCP servers advertise capabilities (tools, resources, sampling). Aidos maps these to Tool Broker interface:

```
MCP Tool: "get_github_issues"
  Input: { repo: String, state: String }
  Output: { issues: List<Issue> }
  
Aidos Tool Broker:
  Tool: "github_mcp"
  Capability: "github:list_issues"
  invoke(capability_id, request) →
    Call MCP server "get_github_issues"
    Return result
```

### Request/Response Flow

```
Session requests: "List GitHub issues for org/repo"

Tool Broker:
  1. Routes to tool "github_mcp"
  2. Maps to MCP tool "get_github_issues"
  3. Sends MCP request over transport
  4. MCP server executes
  5. Returns result
  6. Tool Broker returns to session
  7. Publishes ToolCompleted event
```

### MCP Resources and Sampling

MCP supports resources (files, config) and sampling (prompt templates). Aidos:

- **Resources**: Exposed via filesystem tool (sessions can read MCP resources as files).
- **Sampling**: Not directly used (MCP sampling is for LLM input preparation; Aidos handles this differently).

### Error Handling

MCP server failures are handled by Tool Broker:

```
MCP server crashes:
  Tool Broker detects disconnection
  Logs error
  Publishes ToolFailed event
  Optionally restarts server (configurable)
  Session receives error
```

## Data Model (Conceptual)

```
MCPServerRegistry {
  servers: Map<ServerName, MCPServerConfig>
  
  live_servers: Map<ServerName, MCPServerConnection>
  
  tool_mappings: Map<MCPToolId, ToolBrokerCapability>
}

MCPServerConfig {
  name: String
  command: String                   # How to start server
  transport: String                 # "stdio" or "http"
  
  endpoint_url: String?             # For HTTP transport
  auth: Map<String, Any>?           # Authentication config
  env: Map<String, String>?         # Environment variables
  
  startup_timeout: Duration
  auto_restart: Boolean
}

MCPServerConnection {
  server_id: String
  transport: TransportConnection
  status: ConnectionStatus          # "connected", "disconnected"
  
  capabilities: List<MCP Capability>
  
  connected_at: Timestamp
  last_heartbeat: Timestamp
}
```

## Security

MCP servers operate with Tool Broker security:

1. **Permission checks**: MCP capabilities require Tool Broker permissions.
2. **Isolation**: MCP servers run in separate processes (with stdio transport).
3. **Audit logging**: All MCP calls logged through Tool Broker.
4. **Credential management**: MCP auth handled via project config (encrypted).

## MVP Scope

MVP includes:

1. **Stdio transport**: Start and communicate with MCP servers via stdio.
2. **Capability discovery**: Read MCP capabilities from server.
3. **Tool mapping**: Map MCP tools to Tool Broker interface.
4. **Error handling**: Handle server crashes, timeouts.
5. **Logging**: Log MCP server operations.

Not included:

- HTTP transport (future, less critical for MVP).
- MCP resource integration (future).
- MCP sampling (future).
- Server plugins (future, RFC-0060).

## Future Work

### HTTP Transport

Support MCP servers over HTTP with SSE.

### MCP Server Building

Aidos publishes SDK for building MCP servers:

```python
from aidos.mcp import Server

server = Server("my-tool")

@server.tool()
def process_data(input_data: str) -> str:
    return f"Processed: {input_data}"

server.run()
```

### MCP Resource Support

Integrate MCP resources into Knowledge Engine:

```
MCP server provides: "company_handbook" resource
Knowledge Engine indexes: handbook content
Sessions can search: handbook via semantic search
```

### MCP Marketplace

Curated list of MCP servers compatible with Aidos:

```
"GitHub MCP" (4.8★)
"Stripe MCP" (4.6★)
"Linear MCP" (4.7★)
```

### MCP Debugging

Tools to debug MCP server issues:

```
aidos mcp test github-server
  → Start server
  → List capabilities
  → Attempt sample tool call
  → Report results
```

## Open Questions

- Should Aidos build an MCP server that exposes itself (allowing other AI systems to use Aidos tools)?
- Should MCP servers be allowed to have side effects (write files, change state)?
- How should MCP authentication be handled (especially OAuth flows)?
- Should there be a "MCP server trust" model (verified vs. unverified)?
- Should MCP servers be sandboxed (limit file access, network)?
- How should version conflicts be handled (two MCP servers providing same tool)?
